//! Benchmarks for the sponsored transaction pallet.
//!
//! Benchmark-derived weights are intentionally deferred for the first implementation pass. The
//! pallet currently ships manual placeholder weights in `weights.rs` and should gain full FRAME
//! benchmarks before being treated as production-complete.
